# NextEditor

Workspace for the `next-editor` package and a demo Next.js application.

## Structure

- `packages/next-editor`: installable package
- `apps/demo`: local demo app for development
- `reference`: build spec and notes

## Commands

```bash
pnpm install
pnpm dev
pnpm build
pnpm lint
```
