# AGENTS

## Ownership

`packages/next-editor` is the canonical home for functionality that should ship
to downstream projects.

## Expectations

- Add reusable React components here.
- Add package-owned CSS assets here.
- Add reusable server helpers here when they are part of the package story.
- Keep consumer apps responsible for wiring routes, auth, and persistence around
  those exported APIs.
